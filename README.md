# YouTube Factory

This project is a scaffold for the YouTube factory application.

## Quickstart

1.  Copy `.env.example` to `.env`:
    ```bash
    cp .env.example .env
    ```

2.  Start services:
    ```bash
    docker compose up --build
    ```

3.  Run tests:
    ```bash
    make test
    ```
